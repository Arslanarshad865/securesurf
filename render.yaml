services:
  - type: web
    name: securesurf
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn securesurf.wsgi:application
    autoDeploy: true
